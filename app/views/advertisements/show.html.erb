<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @advertisement.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @advertisement.content %>
</p>

<% if @current_user && @current_user.role == "admin" && @advertisement.state == 'waiting' %>
  <%= link_to 'Publier', "/advertisements/#{@advertisement.id}/publish" %>
<% end %>

<%= link_to 'Edit', edit_advertisement_path(@advertisement) %> |
<%= link_to 'Back', advertisements_path %>
